package com.heck.studentsorting;

import java.util.Arrays;

public class PersonalSort extends Student{
	
	public String selectionSort(String rollCall[]) {

		int indexSmallest; 
		int temp; //Temporary variable for swapping values
		
		for (int i = 0; i < rollCall.length - 1)
	}
}
